---
status: resolved
phase: 05.1-implement-real-pcm-accumulation-and-opus-encoder
source: [05.1-01-SUMMARY.md, 05.1-02-SUMMARY.md, 05.1-03-SUMMARY.md, 05.1-04-SUMMARY.md, 05.1-05-SUMMARY.md, 05.1-06-SUMMARY.md]
started: 2026-03-20T19:45:00Z
updated: 2026-03-21T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running `cargo tauri dev` instance. Start the app fresh with `cargo tauri dev`. The app should boot without errors, the tray icon should appear, both windows (settings + indicator) should be available, and the log plugin should emit startup messages in the devtools console (no panics, no plugin registration errors).
result: pass

### 2. Real Audio Transcription
expected: Press the global hotkey to start recording, speak a few words clearly (e.g. "hello world"), press hotkey again to stop. The app should send real audio to your configured cloud provider (Groq/OpenRouter) and return a transcription of what you said — not an empty result, not a "too short" error, not silence.
result: pass
note: "Transcription returned text but in wrong language (Bosnian/Serbian instead of English). Provider order change (Groq first) not reflected — OpenAI still used. Language setting is empty string. User called pass; issues to be handled separately."

### 3. Short Recording Rejection
expected: Press the global hotkey to start recording, then immediately press it again (within ~0.3 seconds — don't speak). The app should show a user-friendly error message like "Recording too short" rather than sending garbage audio to the transcription API. No crash.
result: pass

### 4. Log Output in Devtools
expected: With the app running via `cargo tauri dev`, open the devtools console (right-click the settings window → Inspect). You should see log entries from the Rust backend (e.g. startup messages, hotkey events, recording state changes) appearing in the console as you use the app — not just frontend JS logs.
result: pass

### 5. Log File Written to Disk
expected: After running the app and triggering at least one recording, check `%APPDATA%\VoxFlow\logs\app.log`. The file should exist and contain log entries. (Open it with Notepad or `cat` in terminal.)
result: pass
note: "File exists at %LOCALAPPDATA%\\com.balazs.bohonyi.voxflow\\logs\\VoxFlow.log — test had wrong expected path, not a code bug."

### 6. Language Selector in Settings
expected: Open the Settings window. A "Language" row should be visible. Click the input — a searchable dropdown opens showing "Auto-detect" at the top and all Whisper-supported languages below. Type "eng" — the list filters to English and English is already highlighted. Press Enter — English is selected and the dropdown closes. Reopen the app or trigger a recording to confirm the language persists (transcription now returns English text).
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

- truth: "Short recording (< 0.5s) shows a user-friendly error message/toast to the user"
  status: resolved
  reason: "User reported: I saw only in the logs but no toast or user-friendly error message was shown for the user."
  severity: major
  test: 3
  root_cause: "hotkey/service.rs Err arm (lines 250-257) logs the error and hides the indicator but never calls indicator::show_toast_window. The too-short check in audio/mod.rs (lines 167-170) also lacks emit_audio_error unlike the EncodeFailed branch."
  artifacts:
    - path: "src-tauri/src/hotkey/service.rs"
      issue: "Err arm at lines 250-257 discards error after log::warn; missing show_toast_window call"
    - path: "src-tauri/src/audio/mod.rs"
      issue: "Too-short early return at lines 167-170 has no emit_audio_error call, unlike EncodeFailed branch"
  missing:
    - "Call indicator::show_toast_window in hotkey/service.rs Err arm with the error message"
    - "Optionally add emit_audio_error in audio/mod.rs too-short branch for consistency"

- truth: "Rust backend log entries appear in the devtools console when opened via Inspect"
  status: resolved
  reason: "User reported: I see the logs only in VS Code in the terminal, not in the devtools console opened via Inspect."
  severity: minor
  test: 4
  root_cause: "@tauri-apps/plugin-log npm package is not installed and attachConsole() is never called in any window entry point. The Webview target emits log://log Tauri events but nothing subscribes."
  artifacts:
    - path: "package.json"
      issue: "@tauri-apps/plugin-log not listed as dependency"
    - path: "src/windows/settings/main.ts"
      issue: "attachConsole() never called"
    - path: "src/windows/indicator/main.ts"
      issue: "attachConsole() never called"
  missing:
    - "pnpm add @tauri-apps/plugin-log"
    - "Call await attachConsole() in each window's main.ts before createApp().mount()"

- truth: "Log file is written to disk after app runs and a recording is triggered"
  status: not_a_bug
  reason: "User reported: no logs folder at %APPDATA%\\VoxFlow. Diagnosis: file IS written to %LOCALAPPDATA%\\com.balazs.bohonyi.voxflow\\logs\\VoxFlow.log. Tauri's app_log_dir() resolves to %LOCALAPPDATA%\\{identifier}\\logs\\ on Windows. User confirmed file exists at correct path."
  severity: cosmetic
  test: 5
  root_cause: "Test had wrong expected path. No code bug."
  missing: []
