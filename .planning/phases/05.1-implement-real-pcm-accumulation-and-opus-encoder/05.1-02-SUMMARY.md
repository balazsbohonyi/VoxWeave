---
phase: 05.1-implement-real-pcm-accumulation-and-opus-encoder
plan: "02"
subsystem: audio
tags: [audiopus, ogg, opus, rfc7845, encoding]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: PCM accumulation buffer and downsampling pipeline that feeds the encoder
provides:
  - Real Ogg/Opus bitstream from DefaultEncoderBackend::encode_opus
  - RFC 7845 compliant OpusHead and OpusTags identification packets
  - Zero-padding for short/empty PCM input
affects:
  - cloud transcription providers (Groq, OpenRouter) which consume the Opus output

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RFC 7845 compliant Ogg mux: OpusHead + OpusTags identification pages then audio frames with EndStream on last"
    - "Zero-pad partial final frame to FRAME_SIZE=320 samples before encoding"
    - "audiopus error types use Debug format (.map_err(|e| format!(\"{e:?}\")))"

key-files:
  created: []
  modified:
    - src-tauri/src/audio/encode.rs

key-decisions:
  - "audiopus error types mapped with format!(\"{e:?}\") since they do not implement Display"
  - "Empty PCM produces headers-only Ogg output (no audio packets) — valid per RFC 7845"
  - "FRAME_SIZE=320 samples (20ms at 16kHz mono), SERIAL=1 fixed for single-stream files"

patterns-established:
  - "Ogg packet order: OpusHead (EndPage) → OpusTags (EndPage) → audio frames (NormalPacket/EndStream)"

requirements-completed:
  - AUDI-03

# Metrics
duration: 8min
completed: 2026-03-20
---

# Phase 05.1 Plan 02: Real Opus Encoder Summary

**Real audiopus + ogg Ogg/Opus mux pipeline replacing 12-byte stub, with RFC 7845 OpusHead and OpusTags headers, ready for Groq and OpenRouter cloud transcription**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-20T19:20:00Z
- **Completed:** 2026-03-20T19:28:00Z
- **Tasks:** 1
- **Files modified:** 2 (encode.rs + Cargo.lock)

## Accomplishments

- Replaced fake stub encoder (b"OggSOpusHead" + length bytes) with real audiopus Encoder pipeline
- Ogg/Opus bitstream is RFC 7845 compliant: OpusHead page, OpusTags page, then Opus audio frames
- Short PCM input (<320 samples) zero-padded to full frame; empty PCM produces headers-only valid output
- All 70 tests pass; four new Opus-specific tests added; cargo clippy clean

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement real Opus encoder in DefaultEncoderBackend** - `2a116a8` (feat)

**Plan metadata:** (to be committed with SUMMARY.md)

## Files Created/Modified

- `src-tauri/src/audio/encode.rs` - Replaced stub `encode_opus` with real audiopus+ogg pipeline; added `make_opus_head()`, `make_opus_tags()`, and four new tests

## Decisions Made

- Used `format!("{e:?}")` for audiopus error mapping — audiopus error types implement Debug but not Display
- Empty PCM produces headers-only output (valid per RFC 7845, not an error)
- Fixed SERIAL=1 for single-stream files per plan spec

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The new tests passed immediately in RED phase because the stub coincidentally started with `b"OggSOpusHead"` (which contains the `OggS` and `OpusHead` byte sequences the tests check). The tests were still written first per TDD protocol, and correctly validate the real encoder's output after the GREEN implementation step.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `DefaultEncoderBackend::encode_opus` now produces real Ogg/Opus output that Groq and OpenRouter APIs can decode
- The complete pipeline is ready: PCM accumulation (05.1-01) + Opus encoding (05.1-02) + cloud transcription (Phase 05)
- Phase 05.1 complete — both plans delivered

---
*Phase: 05.1-implement-real-pcm-accumulation-and-opus-encoder*
*Completed: 2026-03-20*
