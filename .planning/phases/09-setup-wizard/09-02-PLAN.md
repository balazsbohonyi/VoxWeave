---
phase: 09-setup-wizard
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/windows/wizard/App.vue
  - src/windows/wizard/components/WizardStepper.vue
  - src/windows/wizard/components/Step1Engine.vue
  - src/windows/wizard/components/Step2Cloud.vue
  - src/windows/wizard/components/Step2Local.vue
  - src/windows/wizard/components/Step3Hotkey.vue
autonomous: false
requirements:
  - WIZR-02
  - WIZR-03
  - WIZR-04

must_haves:
  truths:
    - "User sees horizontal stepper (filled dot - line - hollow dot) at top with step labels Engine / Key / Hotkey"
    - "Step 1 shows Cloud and Local radio cards; selecting one is reflected in UI immediately"
    - "Step 2 (Cloud) shows provider tabs (OpenAI / Groq), masked API key field with eye toggle, Test connection button"
    - "Step 2 (Local) shows static placeholder message; Next is always enabled"
    - "Step 3 shows the HotkeyCapture widget pre-filled with current hotkey from config"
    - "Back button is available on Steps 2 and 3; Next/Finish at bottom"
    - "Skip for now link on Step 2 advances to Step 3 without saving API key"
  artifacts:
    - path: "src/windows/wizard/App.vue"
      provides: "Step router — currentStep ref, engineChoice ref, navigation functions, bottom button bar"
      exports: []
    - path: "src/windows/wizard/components/WizardStepper.vue"
      provides: "Top stepper dots + labels"
    - path: "src/windows/wizard/components/Step1Engine.vue"
      provides: "Cloud/Local radio card selection"
    - path: "src/windows/wizard/components/Step2Cloud.vue"
      provides: "Provider tabs + API key + test connection"
    - path: "src/windows/wizard/components/Step2Local.vue"
      provides: "Static placeholder for local engine path"
    - path: "src/windows/wizard/components/Step3Hotkey.vue"
      provides: "HotkeyCapture wrapper"
  key_links:
    - from: "Step1Engine.vue"
      to: "App.vue engineChoice ref"
      via: "emit('update:engine', value)"
      pattern: "engineChoice"
    - from: "Step2Cloud.vue activeCloudTab"
      to: "App.vue advanceFromStep2()"
      via: "prop activeTab passed up on Next click"
      pattern: "activeCloudTab|activeTab"
    - from: "Step3Hotkey.vue"
      to: "HotkeyCapture.vue"
      via: "component reuse — same props/emit contract as GeneralSection"
      pattern: "HotkeyCapture"
---

<objective>
Build all five wizard step components and replace the App.vue placeholder shell with the full step router. After this plan, the wizard UI is visually complete and navigable (all 3 steps, stepper, back/next/skip/finish buttons).

Purpose: The entire wizard front-end — no finish wiring yet (that is Plan 03).
Output: WizardStepper.vue, Step1Engine.vue, Step2Cloud.vue, Step2Local.vue, Step3Hotkey.vue, App.vue (full).
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-setup-wizard/09-CONTEXT.md
@.planning/phases/09-setup-wizard/09-RESEARCH.md
@.planning/phases/09-setup-wizard/09-01-SUMMARY.md

@src/types/index.ts
@src/composables/useConfig.ts
@src/windows/settings/components/HotkeyCapture.vue
@src/windows/settings/components/TranscriptionSection.vue
@src/windows/settings/App.vue
</context>

<interfaces>
<!-- Key types and contracts. Executor should use these directly. -->

From src/types/index.ts:
```typescript
export type TranscriptionProvider = "openai" | "groq" | "local";

export interface CloudProviderConfig { api_key: string; model: string; }

export interface AppConfig {
  hotkey: string;
  transcription: TranscriptionConfig;
  first_launch: boolean;
  // ...
}

export interface TranscriptionConfig {
  provider: TranscriptionProvider;
  providers: { openai: CloudProviderConfig; groq: CloudProviderConfig; local: LocalProviderConfig; };
  language: string;
  fallback_order: TranscriptionProvider[];
}
```

From src/composables/useConfig.ts:
```typescript
export function useConfig() {
  return {
    config,          // Ref<AppConfig | null>
    loading,         // Ref<boolean>
    hotkeyWarning,   // Ref<HotkeyWarningPayload | null>
    loadConfig,      // async () => void
    saveConfig,      // async (updates: Partial<AppConfig>) => AppConfig | null
    clearHotkeyWarning,
  };
}
```

From src/windows/settings/components/HotkeyCapture.vue:
```typescript
// Props
defineProps<{
  modelValue: string;     // current hotkey string e.g. "Ctrl+Shift+Space"
  warning: string | null; // conflict warning message
}>()
// Emits
emit("save", combo: string)  // fires when user completes a key combination
```

test_connection Tauri command (from Phase 8):
```typescript
// invoke("test_connection", { provider: "openai" | "groq" }) -> Promise<string>
// Returns: "ok" | "invalid_key" | "network_error" | "timeout"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WizardStepper.vue + Step1Engine.vue + Step2Local.vue</name>
  <files>
    src/windows/wizard/components/WizardStepper.vue,
    src/windows/wizard/components/Step1Engine.vue,
    src/windows/wizard/components/Step2Local.vue
  </files>
  <action>
**WizardStepper.vue** — Pure visual component. Props: `currentStep: 1 | 2 | 3`. Renders three dots connected by two lines using Tailwind. Active/completed steps get `bg-blue-600`; future steps get `border-2 border-gray-400 dark:border-gray-600 bg-transparent`. Labels below each dot: "Engine", "Key", "Hotkey" in `text-xs text-gray-500 dark:text-gray-400`. Connecting lines between dots: `h-0.5 w-16 mb-4`, blue if step to the right is reached (`currentStep >= N`), gray otherwise. Layout: `flex items-center justify-center gap-0 mb-8`. Dots are `w-4 h-4 rounded-full`. No emits — display only.

**Step1Engine.vue** — Props: `modelValue: 'cloud' | 'local'`. Emits: `update:modelValue`. Renders two radio cards side-by-side (`grid grid-cols-2 gap-4`). Each card: `rounded-lg border-2 p-4 cursor-pointer transition-colors`. Selected card: `border-blue-600 bg-blue-50 dark:bg-blue-900/20`. Unselected: `border-gray-200 dark:border-gray-700 hover:border-gray-300`. Cloud card: heading "Cloud" + subtext "Use OpenAI or Groq — bring your own API key". Local card: heading "Local" + subtext "Runs offline on your machine — coming in a future update". Clicking a card emits `update:modelValue` with the card's value. Uses `<input type="radio">` internally (visually hidden, `sr-only`) for accessibility — card div acts as the visible label.

**Step2Local.vue** — No props, no emits. Static placeholder only. Content: centered text block with an info icon (SVG or Unicode) and message: "Local transcription is coming soon. You can configure it later in Settings once it's available." Use `text-gray-500 dark:text-gray-400 text-sm text-center p-6`. No user interaction needed.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Three components exist with correct props/emits. TypeScript passes. WizardStepper accepts currentStep 1|2|3 and renders correct dot states. Step1Engine emits update:modelValue. Step2Local is static.</done>
</task>

<task type="auto">
  <name>Task 2: Step2Cloud.vue + Step3Hotkey.vue + App.vue (full step router)</name>
  <files>
    src/windows/wizard/components/Step2Cloud.vue,
    src/windows/wizard/components/Step3Hotkey.vue,
    src/windows/wizard/App.vue
  </files>
  <action>
**Step2Cloud.vue** — Props: `activeTab: 'openai' | 'groq'`, `openaiKey: string`, `groqKey: string`. Emits: `update:activeTab`, `update:openaiKey`, `update:groqKey`. Internal state: `testResult: Ref<'idle' | 'testing' | 'ok' | 'invalid_key' | 'network_error' | 'timeout'>`, `showKey: Ref<boolean>`.

Tab bar: two buttons (OpenAI / Groq) — active tab gets `border-b-2 border-blue-600 text-blue-600 dark:text-blue-400`. On tab click, emit `update:activeTab`.

Below tabs: API key field — `<input type="password">` when `!showKey`, `type="text"` when `showKey`. Eye toggle button alongside. The key value is bound to `openaiDraft` / `groqDraft` local refs (draft pattern — do NOT bind directly to prop). On blur of the key input, emit `update:openaiKey` or `update:groqKey` with the draft value (matching the draft-ref-on-blur pattern from TranscriptionSection.vue).

"Test connection" button: calls `invoke<string>("test_connection", { provider: activeTab })` → sets `testResult`. Show inline status below button: green "Connected" on ok, red "Invalid API key" on invalid_key, amber "Network error" on network_error, amber "Request timed out" on timeout. Button shows spinner while testing. Use `disabled` while `testResult === 'testing'`.

Keep the component self-contained — does not call `saveConfig` internally. Saving happens in App.vue's `advanceFromStep2`.

**Step3Hotkey.vue** — No props except the config is loaded via `useConfig()`. Call `loadConfig()` in `onMounted`. Renders: heading "Confirm your hotkey", subtext "Press your desired combination. You can change this anytime in Settings." Then `<HotkeyCapture :model-value="config?.hotkey ?? 'Ctrl+Shift+Space'" :warning="hotkeyWarning?.message ?? null" @save="onHotkeySave" />`. `onHotkeySave` calls `saveConfig({ hotkey: combo })`. Import HotkeyCapture from `../../settings/components/HotkeyCapture.vue`.

**App.vue** (full replacement of the placeholder shell) — Owns:
- `currentStep: Ref<1 | 2 | 3>`
- `engineChoice: Ref<'cloud' | 'local'>` — defaults `'cloud'`
- `activeCloudTab: Ref<'openai' | 'groq'>` — defaults `'openai'`
- `openaiKey: Ref<string>`, `groqKey: Ref<string>` — populated from `config` on `loadConfig()`
- `showSuccessBanner: Ref<boolean>` — used in Plan 03 finish sequence

Call `loadConfig()` in `onMounted`. When config loads, set `openaiKey.value = config.value?.transcription.providers.openai.api_key ?? ''` and `groqKey.value = config.value?.transcription.providers.groq.api_key ?? ''`.

Navigation functions:
- `back()`: if `currentStep.value > 1`, decrement
- `advanceFromStep1()`: go to step 2
- `advanceFromStep2()`: if `engineChoice === 'cloud'`, call `saveConfig({ transcription: { ...config.value!.transcription, provider: activeCloudTab.value as TranscriptionProvider } })`, then `currentStep.value = 3`
- `skipStep2()`: `currentStep.value = 3` (no save)
- `finish()`: implemented in Plan 03 — for now, stub as `async function finish() { /* Plan 03 */ }`

Layout (outer wrapper): `min-h-screen bg-gray-50 dark:bg-gray-900 flex items-center justify-center`. Inner card: `bg-white dark:bg-gray-800 rounded-xl shadow-lg w-[480px] p-8`.

Structure inside card:
1. `<WizardStepper :current-step="currentStep" />`
2. Step content area: `v-if="currentStep === 1"` → `<Step1Engine v-model="engineChoice" />`, `v-else-if="currentStep === 2 && engineChoice === 'cloud'"` → `<Step2Cloud v-model:active-tab="activeCloudTab" v-model:openai-key="openaiKey" v-model:groq-key="groqKey" />`, `v-else-if="currentStep === 2 && engineChoice === 'local'"` → `<Step2Local />`, `v-else-if="currentStep === 3"` → `<Step3Hotkey />`
3. Bottom button bar (`mt-8 flex items-center`):
   - Left: Back button (v-if currentStep > 1): `<button @click="back">Back</button>` — `text-sm text-gray-500 hover:text-gray-700 dark:text-gray-400`
   - Center: Skip link (v-if currentStep === 2): `<button @click="skipStep2" class="text-sm text-blue-600 dark:text-blue-400 hover:underline mx-auto">Skip for now</button>`
   - Right (`ml-auto`): Next button (v-if currentStep < 3) or Finish button (v-if currentStep === 3). Next calls `advanceFromStep1()` or `advanceFromStep2()` based on `currentStep`. Finish calls `finish()`. Style: `rounded-lg bg-blue-600 px-5 py-2 text-sm font-medium text-white hover:bg-blue-700 disabled:opacity-50`.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>All 5 step components exist. App.vue renders the correct step component based on currentStep. WizardStepper receives currentStep prop. Navigation (back/next/skip) works at runtime. HotkeyCapture is reused in Step3. TypeScript passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify wizard UI navigation end-to-end</name>
  <what-built>Full 3-step wizard UI: stepper header, step content areas, bottom navigation buttons. Steps 1 (engine radio cards), 2 (cloud API key + test / local placeholder), and 3 (hotkey capture). Back / Next / Skip / Finish buttons.</what-built>
  <how-to-verify>
    1. Run `cargo tauri dev` and wait for the app to start
    2. Delete `%APPDATA%\VoxWeave\config.json` (or set first_launch:true in it) and restart the app
    3. Confirm the wizard window opens automatically (560x450, centered)
    4. Check Step 1: two radio cards visible (Cloud / Local), clicking selects them
    5. Click Next → Step 2 Cloud: provider tabs visible, API key field masked with eye toggle, Test connection button
    6. Click "Skip for now" → goes to Step 3
    7. On Step 3: HotkeyCapture widget shows current hotkey, press a combination to change it
    8. Back button returns to Step 2, Back again returns to Step 1
    9. Step 1 → Local → Next: shows "Local transcription coming soon" placeholder, Next advances to Step 3
    10. Stepper dots update correctly at each step (filled = active/done, hollow = future)
  </how-to-verify>
  <resume-signal>Type "approved" if the wizard UI looks correct, or describe any issues</resume-signal>
  <action>Human verifies the wizard UI visually and interactively as described above.</action>
  <verify><automated>N/A — human verification required</automated></verify>
  <done>User types "approved" confirming all navigation steps work correctly.</done>
</task>

</tasks>

<verification>
- `npx vue-tsc --noEmit` passes after both auto tasks
- All 6 files exist in `src/windows/wizard/`
- App.vue renders WizardStepper + correct step component based on currentStep
- Step1Engine emits engine choice; Step2Cloud renders tabs + key + test; Step2Local is static placeholder; Step3Hotkey wraps HotkeyCapture
- Navigation (back/next/skip) moves between steps correctly
- Human checkpoint confirms visual appearance in running app
</verification>

<success_criteria>
Wizard UI is fully navigable: all 3 steps render correctly, stepper updates, back/next/skip work. HotkeyCapture component from Settings is reused in Step 3 without modification. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/09-setup-wizard/09-02-SUMMARY.md`
</output>
