---
phase: 08-settings-ui
plan: 05
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - src/windows/settings/components/InjectionSection.vue
  - src/windows/settings/App.vue
autonomous: false
requirements: [SETT-01, SETT-06, SETT-07]

must_haves:
  truths:
    - "Settings window shows all four sections: General, Audio, Transcription, Injection"
    - "Injection section: method selector persists immediately; speed selector appears only for Keystrokes; auto-fallback checkbox persists"
    - "All settings persist immediately across app restart (no save button anywhere)"
    - "Human can verify the full settings flow end-to-end"
  artifacts:
    - path: "src/windows/settings/components/InjectionSection.vue"
      provides: "Method selector, conditional speed selector, auto-fallback checkbox"
    - path: "src/windows/settings/App.vue"
      provides: "All four sections rendered; InjectionSection wired"
  key_links:
    - from: "InjectionSection method selector"
      to: "saveConfig({ injection: { ...config.injection, mode: selectedMode } })"
      via: "onChange immediate save"
      pattern: "saveConfig.*injection.*mode"
    - from: "speed selector v-if"
      to: "config.injection.mode === 'keystroke'"
      via: "conditional render"
      pattern: "keystroke"
---

<objective>
Build InjectionSection.vue, wire it into App.vue completing all four sections, then do a human-verify checkpoint to confirm the full settings UI works end-to-end.

Purpose: Completes SETT-01 (all four sections present), SETT-06 (injection controls), and SETT-07 verification (settings persist).
Output: Fully assembled settings window. Human confirms all sections render and settings persist.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/types/index.ts
@src/composables/useConfig.ts
@src/windows/settings/App.vue
@.planning/phases/08-settings-ui/08-03-SUMMARY.md
@.planning/phases/08-settings-ui/08-04-SUMMARY.md

<interfaces>
<!-- InjectionConfig from types/index.ts -->
```typescript
export type InjectionMode = "flash_paste" | "keystroke" | "clipboard";
export type KeystrokeSpeed = "slow" | "normal" | "fast";
export interface InjectionConfig {
  mode: InjectionMode;
  keystroke_speed: KeystrokeSpeed;
  auto_fallback: boolean;
  paste_delay_ms: number;   // not exposed in UI (too technical)
}
```

<!-- Correct save pattern for injection section -->
```typescript
// Mode change:
await saveConfig({ injection: { ...config.value!.injection, mode: newMode } });
// Speed change:
await saveConfig({ injection: { ...config.value!.injection, keystroke_speed: newSpeed } });
// Fallback toggle:
await saveConfig({ injection: { ...config.value!.injection, auto_fallback: checked } });
// Note: paste_delay_ms is NOT surfaced in the UI — preserve via spread
```

<!-- Speed selector conditional display (from RESEARCH.md + CONTEXT.md) -->
```html
<!-- Speed selector only shown when Keystrokes mode is selected -->
<div v-if="config.injection.mode === 'keystroke'" class="...">
  <!-- speed radio or select -->
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InjectionSection.vue + wire into App.vue</name>
  <files>src/windows/settings/components/InjectionSection.vue, src/windows/settings/App.vue</files>
  <action>
**InjectionSection.vue:**
Calls `useConfig()` for `config` and `saveConfig`.

Controls:
1. `<SectionDivider title="Injection" />`
2. Method selector — label "Injection method":
   - Three radio buttons or a `<select>` (use radio buttons for clearer UX):
     - `flash_paste` → "FlashPaste (recommended)"
     - `keystroke` → "Simulated keystrokes"
     - `clipboard` → "Clipboard only"
   - On change: `saveConfig({ injection: { ...config.injection, mode: value } })`
   - Bind `:checked` / `:value` from `config.injection.mode`
3. Speed selector (conditional on `config.injection.mode === 'keystroke'`):
   - Label "Typing speed"
   - Three radio buttons: `slow` → "Slow (10ms/char)", `normal` → "Normal (5ms/char)", `fast` → "Fast (2ms/char)"
   - On change: `saveConfig({ injection: { ...config.injection, keystroke_speed: value } })`
   - Plain `v-if` conditional — no animation library
4. Auto-fallback checkbox:
   - Label "Automatically fall back to next method if injection fails"
   - `:checked="config.injection.auto_fallback"` `@change="saveConfig({ injection: { ...config.injection, auto_fallback: $event.target.checked } })"`

Note: `paste_delay_ms` is preserved via spread in all save calls but never shown in the UI.

Styling: consistent with other sections. Radio button rows: `flex items-center gap-2 py-1`. Section uses `space-y-4`.

**App.vue update:**
Replace `<div id="injection-stub"></div>` with `<InjectionSection />`. Import InjectionSection. The settings window now has all four sections rendered.
  </action>
  <verify>
    <automated>cd D:/develop/projects/VoxFlow && npx vue-tsc --noEmit && npm run lint</automated>
  </verify>
  <done>InjectionSection.vue renders with method radios, conditional speed radios, and auto-fallback checkbox. All wired to saveConfig. TypeScript and lint pass. All four sections present in App.vue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full settings window end-to-end</name>
  <action>
Run `cargo tauri dev` and open the settings window. Follow the verification steps in `<how-to-verify>`. This is a visual and functional review — no code changes required unless issues are found.
  </action>
  <verify>
    <automated>cd D:/develop/projects/VoxFlow && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Human confirms all four sections render correctly and settings persist across app restart.</done>
  <what-built>
Full settings window with all four sections:
- General: HotkeyCapture widget, Launch on startup toggle, Minimize-to-tray row
- Audio: Microphone dropdown, Auto-stop on silence toggle + duration input
- Transcription: Cloud/Local toggle, OpenAI/Groq tabs with API key, model, test-connection, set-as-active, language hint; Local model stub cards
- Injection: Method selector (FlashPaste/Keystrokes/Clipboard), Speed selector (Keystrokes only), Auto-fallback checkbox
  </what-built>
  <how-to-verify>
Run `cargo tauri dev` and open the settings window (double-click tray icon or right-click → Settings).

1. **Layout**: Window is wider (~480px). Scroll down — all four sections visible with heading dividers.

2. **General section**:
   - Click the hotkey capture field, hold Ctrl+Shift, press a key — verify live preview shows "Ctrl+Shift+X". Release — verify it saves (open config.json in %APPDATA%/VoxFlow/ to confirm).
   - Press Escape in the hotkey field — verify it cancels (no save, field shows previous value).
   - Press Tab while in the hotkey field — verify focus moves to next control (not captured as "Tab" hotkey).
   - Toggle "Launch on Windows startup" — verify it saves to config.json.

3. **Audio section**:
   - Verify microphone dropdown lists available devices.
   - Toggle "Auto-stop on silence" OFF — verify duration input is greyed out and cannot be typed into.
   - Toggle it ON — verify duration input is enabled.
   - Change duration to "2.0" — verify config.json shows `vad_silence_ms: 2000` after ~300ms.

4. **Transcription section**:
   - Verify OpenAI tab is shown by default (Cloud mode).
   - Click the Groq tab — verify tab switches without changing the active provider badge.
   - Click "Set as active" on Groq tab — verify Groq tab now shows "Active" badge.
   - Enter a test API key in OpenAI, click "Test connection" — verify spinner appears then a result message (error expected with fake key).
   - Verify API key is masked by default; eye icon reveals it.
   - Verify model dropdown for OpenAI shows ["whisper-1", "gpt-4o-transcribe", "gpt-4o-mini-transcribe"].
   - Click "Local" toggle — verify local model stub cards appear for Tiny/Base/Small/Medium with disabled Download buttons.

5. **Injection section**:
   - Select "Simulated keystrokes" — verify speed selector appears below.
   - Select "FlashPaste" — verify speed selector disappears.
   - Toggle auto-fallback — verify saves to config.json.

6. **Persistence**: Restart the app (quit from tray, reopen). Verify all changed settings were restored from disk.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification covers all SETT-01 through SETT-07 acceptance criteria.
</verification>

<success_criteria>
- All four sections render in the settings window
- Speed selector appears only when Keystrokes mode is selected
- Settings survive app restart (SETT-07)
- No TypeScript or lint errors
- Human approves the full UI in the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08-settings-ui/08-05-SUMMARY.md`
</output>
