---
phase: 10-local-transcription
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/windows/settings/components/TranscriptionSection.vue
  - src/windows/wizard/components/Step2Local.vue
  - src-tauri/tauri.conf.json
autonomous: true
gap_closure: true
requirements:
  - LOCL-02
  - LOCL-03

must_haves:
  truths:
    - "After deleting a downloaded model, clicking Download on that card immediately starts a new download with a visible progress bar"
    - "Wizard Step 2 (local model picker) displays all model cards without a vertical scrollbar"
    - "On downloaded model cards, the Delete (trash) button appears before the Set Active button"
  artifacts:
    - path: "src/windows/settings/components/TranscriptionSection.vue"
      provides: "deleteModel resets activeDownloadId; button order is delete-then-set-active"
    - path: "src/windows/wizard/components/Step2Local.vue"
      provides: "deleteModel resets activeDownloadId; button order is delete-then-set-active"
    - path: "src-tauri/tauri.conf.json"
      provides: "Wizard window height increased to 550"
  key_links:
    - from: "deleteModel()"
      to: "activeDownloadId.value"
      via: "direct assignment to null"
      pattern: "activeDownloadId\\.value = null"
---

<objective>
Close three UAT gaps from 10-UAT.md:
1. (major) Re-download after delete broken — deleteModel() never resets activeDownloadId, so Download button stays permanently disabled.
2. (minor) Wizard window too short — vertical scrollbar appears in Step 2 local model picker.
3. (minor) Button order wrong — Set Active renders before Delete on downloaded model cards.

Purpose: Phase 10 UAT had 3 failures; this plan resolves all of them so the phase can be marked complete.
Output: Two Vue components patched, tauri.conf.json height bumped.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-local-transcription/10-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix re-download after delete (Settings + Wizard)</name>
  <files>src/windows/settings/components/TranscriptionSection.vue, src/windows/wizard/components/Step2Local.vue</files>
  <action>
In BOTH files, add `activeDownloadId.value = null;` as the first statement inside `deleteModel()`, before the state/percent resets and the `invoke("delete_model", ...)` call.

Exact change in TranscriptionSection.vue (around line 302):
```
async function deleteModel(modelId: string) {
  activeDownloadId.value = null;          // <-- ADD THIS LINE
  // Clear state immediately so the Active badge never flickers on a card being deleted.
  modelStates.value[modelId] = 'idle';
  ...
```

Exact change in Step2Local.vue (around line 101):
```
async function deleteModel(modelId: string) {
  activeDownloadId.value = null;          // <-- ADD THIS LINE
  // Clear state immediately so the Active badge never flickers on a card being deleted.
  modelStates.value[modelId] = 'idle';
  ...
```

Root cause: `activeDownloadId` is only cleared by download-done/cancelled/error event listeners. A delete fires none of those, so `activeDownloadId` stays non-null, and every Download button remains disabled (`：disabled="activeDownloadId !== null"`).
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>After deleting a downloaded model, clicking Download on the same card shows a progress bar and the download proceeds normally. TypeScript check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Swap button order (delete before set-active) + increase wizard height</name>
  <files>src/windows/settings/components/TranscriptionSection.vue, src/windows/wizard/components/Step2Local.vue, src-tauri/tauri.conf.json</files>
  <action>
**Button order fix — TranscriptionSection.vue (lines 681-701):**
In the `div.flex.items-center.gap-2` container for downloaded-model action buttons, move the trash-icon `<button>` (currently after "Set Active") to appear BEFORE the "Set Active" `<button>`. The resulting order inside the flex container must be: [Delete button] [Set Active button].

**Button order fix — Step2Local.vue (lines 272-292):**
Same swap: move the trash-icon `<button>` before the "Set Active" `<button>` in the downloaded-model action area.

**Wizard window height — src-tauri/tauri.conf.json:**
Find the `"label": "wizard"` window entry (around line 63) and change `"height": 520` to `"height": 550`.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Model cards in both Settings and Wizard show trash icon first, Set Active second. Wizard window is 550px tall. TypeScript check passes.</done>
</task>

</tasks>

<verification>
Run TypeScript check: `npx vue-tsc --noEmit` — must exit 0.
Manual smoke-test (human-verify not required — all changes are mechanical):
- Delete a downloaded model → Download button is immediately clickable → clicking it starts a new download with a visible progress bar.
- Wizard Step 2 shows all model cards without a vertical scrollbar.
- Downloaded model cards show the trash icon to the LEFT of the Set Active button in both Settings and Wizard.
</verification>

<success_criteria>
- `npx vue-tsc --noEmit` passes with exit 0.
- `activeDownloadId.value = null` is the first statement in `deleteModel()` in both Vue files.
- Wizard window height in tauri.conf.json is 550.
- Delete button precedes Set Active button in both component templates.
</success_criteria>

<output>
After completion, create `.planning/phases/10-local-transcription/10-04-SUMMARY.md`
</output>
