---
phase: 10.1-fix-the-build-when-starting-the-exe-file-the-indicator-shows-inside-the-indicator-window-part-of-the-settings-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - src-tauri/tauri.conf.json
  - src/index.html
  - src/settings.html
  - src/wizard.html
  - src/toast.html
autonomous: false
requirements: []

must_haves:
  truths:
    - "Production build produces 4 HTML files under dist/src/"
    - "Indicator window shows the indicator pill UI (not settings content)"
    - "Wizard window shows the wizard flow (not settings content)"
    - "Toast window shows toast notifications (not settings content)"
    - "Settings window continues to open and function correctly"
  artifacts:
    - path: "src/index.html"
      provides: "Indicator HTML entry point"
    - path: "src/settings.html"
      provides: "Settings HTML entry point"
    - path: "src/wizard.html"
      provides: "Wizard HTML entry point"
    - path: "src/toast.html"
      provides: "Toast HTML entry point"
    - path: "vite.config.ts"
      provides: "Multi-page rollupOptions.input covering all 4 entries"
      contains: "rollupOptions"
    - path: "src-tauri/tauri.conf.json"
      provides: "Updated window url fields pointing to src/*.html"
  key_links:
    - from: "vite.config.ts build.rollupOptions.input"
      to: "dist/src/*.html"
      via: "Vite multi-page build"
      pattern: "rollupOptions.*input"
    - from: "tauri.conf.json window url"
      to: "dist/src/*.html"
      via: "Tauri frontendDist resolution"
      pattern: "src/index\\.html|src/settings\\.html"
---

<objective>
Fix the production build so all four Tauri windows load the correct HTML. In dev mode Vite serves all files from the project root (works). In production, Vite builds only `dist/index.html` (no multi-page config), so every window loads settings content.

Purpose: B002 regression — indicator shows settings UI, wizard opens a second settings window.
Output: 4 HTML files moved to src/, Vite multi-page config, tauri.conf.json URLs updated, all four windows load correct content in production .exe.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10.1-fix-the-build-when-starting-the-exe-file-the-indicator-shows-inside-the-indicator-window-part-of-the-settings-window/10.1-CONTEXT.md
@.planning/phases/10.1-fix-the-build-when-starting-the-exe-file-the-indicator-shows-inside-the-indicator-window-part-of-the-settings-window/10.1-RESEARCH.md

<interfaces>
<!-- Current state: 4 HTML files in project root, no rollupOptions.input in vite.config.ts -->

Current vite.config.ts (no build section at all):
```typescript
import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";
import tailwindcss from "@tailwindcss/vite";
// @ts-expect-error process is a nodejs global
const host = process.env.TAURI_DEV_HOST;
export default defineConfig(async () => ({
  plugins: [vue(), tailwindcss()],
  clearScreen: false,
  server: { port: 1420, strictPort: true, host: host || false,
    hmr: host ? { protocol: "ws", host, port: 1421 } : undefined,
    watch: { ignored: ["**/src-tauri/**"] } },
}));
```

Current tauri.conf.json window url fields:
  settings  -> "index.html"
  indicator -> "indicator.html"
  toast     -> "toast.html"
  wizard    -> "wizard.html"

Current HTML files (all in project root):
  index.html     — script src="/src/windows/settings/main.ts"
  indicator.html — script src="/src/windows/indicator/main.ts"
  toast.html     — script src="/src/windows/toast/main.ts"
  wizard.html    — script src="/src/windows/wizard/main.ts"

Target HTML files (move to src/, rename per decisions):
  src/settings.html  (was index.html)
  src/index.html     (was indicator.html — indicator is semantic "main" entry)
  src/toast.html     (was toast.html)
  src/wizard.html    (was wizard.html)

Script src attributes inside HTML files use root-relative paths — NO content changes needed after move.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move HTML files to src/ and configure Vite multi-page build</name>
  <files>src/index.html, src/settings.html, src/wizard.html, src/toast.html, vite.config.ts</files>
  <action>
1. Move and rename the 4 HTML files from project root to src/:
   - Copy `index.html` to `src/settings.html` (Settings entry)
   - Copy `indicator.html` to `src/index.html` (Indicator entry — semantic "main")
   - Copy `toast.html` to `src/toast.html`
   - Copy `wizard.html` to `src/wizard.html`
   - Delete the original 4 files from the project root: `index.html`, `indicator.html`, `toast.html`, `wizard.html`
   - Do NOT modify any content in the HTML files — script src paths like `/src/windows/*/main.ts` are root-relative and Vite rewrites them at build time regardless of HTML file location.

2. Update `vite.config.ts` — add `build.rollupOptions.input` covering all 4 entries:
   - Add `import { resolve } from "node:path";` at the top
   - Add a `build` section inside the config object:
     ```typescript
     build: {
       rollupOptions: {
         input: {
           indicator: resolve(__dirname, "src/index.html"),
           settings: resolve(__dirname, "src/settings.html"),
           wizard:   resolve(__dirname, "src/wizard.html"),
           toast:    resolve(__dirname, "src/toast.html"),
         },
       },
     },
     ```
   - Use `resolve(__dirname, ...)` — NOT `import.meta.dirname` or string concatenation.
   - Keep all existing `server` config unchanged.
   - Keep the `// @ts-expect-error process is a nodejs global` comment.
  </action>
  <verify>
    <automated>cd D:/develop/projects/VoxWeave && ls src/index.html src/settings.html src/wizard.html src/toast.html && ! ls index.html indicator.html toast.html wizard.html 2>/dev/null && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 4 HTML files exist under src/, none remain in project root, vite.config.ts contains rollupOptions.input with all 4 entries, TypeScript typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update tauri.conf.json window URLs to new src/ paths</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
In `src-tauri/tauri.conf.json`, update the `url` field of each window entry under `app.windows`:

- `label: "settings"` — change `url` from `"index.html"` to `"src/settings.html"`
- `label: "indicator"` — change `url` from `"indicator.html"` to `"src/index.html"`
- `label: "toast"` — change `url` from `"toast.html"` to `"src/toast.html"`
- `label: "wizard"` — change `url` from `"wizard.html"` to `"src/wizard.html"`

Use bare relative paths (no leading slash) — Tauri resolves them relative to `frontendDist` (`../dist`).
In dev mode Tauri prepends `devUrl` (e.g. `http://localhost:1420/src/settings.html`) — also correct.
Do NOT change any other window properties (label, title, alwaysOnTop, decorations, etc.).
  </action>
  <verify>
    <automated>cd D:/develop/projects/VoxWeave && npm run build 2>&1 | tail -5 && ls dist/src/index.html dist/src/settings.html dist/src/wizard.html dist/src/toast.html</automated>
  </verify>
  <done>
`npm run build` (Vite build only) completes without errors. `dist/src/` contains all 4 HTML files: `index.html`, `settings.html`, `wizard.html`, `toast.html`. `dist/assets/` contains 4 distinct JS bundles (one per window).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all 4 windows show correct content in production .exe</name>
  <what-built>
    Moved all 4 HTML files to src/, configured Vite multi-page build, updated tauri.conf.json window URLs.
    Automated verification confirmed dist/src/ has all 4 HTML files.
    Now verifying the production .exe works correctly.
  </what-built>
  <how-to-verify>
1. Run `cargo tauri build` from the project root (this builds the full installer .exe).
2. Run the produced .exe from `src-tauri/target/release/VoxWeave.exe` (or the installer).
3. Verify: The app launches, tray icon appears — no window opens on startup.
4. Right-click tray → Settings: the Settings window opens and shows the Settings UI (not a blank or indicator screen).
5. Trigger a recording with the hotkey: the floating indicator pill appears showing the waveform — NOT the settings content.
6. Open Settings → Setup Wizard button: the wizard window opens showing the wizard flow — NOT a second Settings window.
7. Complete or dismiss a transcription: a toast notification appears at the top of the screen (small notification, NOT the settings window).
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 windows show correct content, or describe which window still shows wrong content.</resume-signal>
</task>

</tasks>

<verification>
- `dist/src/index.html` exists (indicator entry)
- `dist/src/settings.html` exists (settings entry)
- `dist/src/wizard.html` exists (wizard entry)
- `dist/src/toast.html` exists (toast entry)
- `dist/assets/` contains 4 distinct JS bundle files (one per window, not a single monolithic bundle)
- No HTML files remain in project root
- `npm run build` exits 0
- `npx vue-tsc --noEmit` exits 0
- Production .exe: all 4 windows load their correct UI content
</verification>

<success_criteria>
The production .exe shows correct content in all 4 windows:
1. Indicator pill shows the waveform/recording UI
2. Settings window shows the settings configuration UI
3. Wizard window shows the onboarding flow
4. Toast window shows toast notifications

`npm run build` produces `dist/src/` with 4 HTML files and no errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-the-build-when-starting-the-exe-file-the-indicator-shows-inside-the-indicator-window-part-of-the-settings-window/10.1-01-SUMMARY.md`
</output>
