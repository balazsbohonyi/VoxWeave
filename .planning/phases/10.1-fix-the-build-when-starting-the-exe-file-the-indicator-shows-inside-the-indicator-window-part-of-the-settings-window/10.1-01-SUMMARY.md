---
phase: 10.1-fix-the-build-when-starting-the-exe-file-the-indicator-shows-inside-the-indicator-window-part-of-the-settings-window
plan: 01
subsystem: ui
tags: [vite, tauri, multi-page-build, rollup]

# Dependency graph
requires:
  - phase: 10-local-transcription
    provides: complete 4-window app with settings, indicator, toast, wizard
provides:
  - Vite multi-page build producing 4 distinct HTML/JS bundles under dist/src/
  - src/index.html (indicator entry), src/settings.html, src/toast.html, src/wizard.html
  - tauri.conf.json window URLs updated to src/*.html paths
affects: [production-build, all-windows]

# Tech tracking
tech-stack:
  added: []
  patterns: [Vite multi-page build via rollupOptions.input, HTML entries co-located with their TS source under src/]

key-files:
  created:
    - src/index.html
    - src/settings.html
    - src/toast.html
    - src/wizard.html
  modified:
    - vite.config.ts
    - src-tauri/tauri.conf.json

key-decisions:
  - "HTML entry files moved to src/ (co-located with TS source) so rollupOptions.input paths are clean relative to project root"
  - "indicator.html renamed to src/index.html — indicator is the semantic main entry (always-on-top, frequent use)"
  - "rollupOptions.input uses resolve(__dirname, ...) not import.meta.dirname or string concatenation"

patterns-established:
  - "Vite multi-page: each Tauri window has its own HTML entry under src/ with rollupOptions.input"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-04-04
---

# Phase 10.1 Plan 01: Fix Multi-Page Vite Build Summary

**Vite multi-page build config added with 4 HTML entries moved to src/ and tauri.conf.json URLs updated — production dist/src/ now has 4 distinct HTML/JS bundles instead of one monolithic settings bundle**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-04T18:03:26Z
- **Completed:** 2026-04-04T18:05:20Z
- **Tasks:** 3 of 3 complete
- **Files modified:** 6

## Accomplishments
- Moved all 4 HTML files from project root to src/ with correct names (index.html=indicator, settings.html=settings)
- Added `build.rollupOptions.input` in vite.config.ts covering all 4 window entries
- Updated all 4 window URL fields in tauri.conf.json from bare filenames to `src/*.html` paths
- `npm run build` produces `dist/src/` with 4 distinct HTML files and 4 distinct JS bundles

## Task Commits

Each task was committed atomically:

1. **Task 1: Move HTML files to src/ and configure Vite multi-page build** - `04398d2` (feat)
2. **Task 2: Update tauri.conf.json window URLs to new src/ paths** - `2ad19d1` (feat)
3. **Task 3: Verify all 4 windows show correct content in production .exe** - human-verified (approved 2026-04-04)

## Files Created/Modified
- `src/index.html` - Indicator window HTML entry (was indicator.html in root)
- `src/settings.html` - Settings window HTML entry (was index.html in root)
- `src/toast.html` - Toast window HTML entry (was toast.html in root)
- `src/wizard.html` - Wizard window HTML entry (was wizard.html in root)
- `vite.config.ts` - Added build.rollupOptions.input with all 4 entries + resolve import
- `src-tauri/tauri.conf.json` - Updated 4 window url fields to src/*.html paths

## Decisions Made
- HTML entries moved to `src/` (not a new top-level `pages/` dir) to stay co-located with the TS source they reference
- `src/index.html` maps to indicator (not settings) because indicator is the "main" always-running UI
- `resolve(__dirname, ...)` used as specified — more robust than string concatenation in Vite config

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 4 windows verified in production .exe: indicator shows waveform pill, settings shows configuration UI, wizard shows onboarding flow, toast shows notifications
- `npm run build` exits 0, `npx vue-tsc --noEmit` exits 0
- B002 regression fully resolved — no further work required on this phase

---
*Phase: 10.1-fix-the-build*
*Completed: 2026-04-04*
